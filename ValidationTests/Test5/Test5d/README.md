# LLM Metric Test Repo

This repo is designed to test an LLM Metric validation stage.

It contains a small C project with nested folders:
- `src/calculator/`
- `src/validation/`
- `include/`

The code is intentionally aligned with the provided prompt so your metric stage should score it reasonably high.
