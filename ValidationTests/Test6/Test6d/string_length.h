/*
 * string_length.h
 *
 * Purpose:
 *   Declare the string_length() function for Test6d.
 *
 * Goal:
 *   Provide the public API for computing the length of a C string.
 */

#ifndef STRING_LENGTH_H
#define STRING_LENGTH_H

/*
 * string_length
 *
 * Return the length of the string pointed to by s.
 */
int string_length(const char *s);

#endif